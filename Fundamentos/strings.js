const escola = "Cod3r"

console.log(escola.charAt(4)) // Buscar o indice no texto
console.log(escola.charCodeAt(3)) // Buscar o codigo aschii no texto
console.log(escola.indexOf("3")) // buscar o indice 3 no texto

console.log(escola.substring(1)) // mostrar do indice até o fim do texto

console.log(escola.replace(3, "e")) // substituir o numero 3 por e
console.log("Escola".concat(escola).concat("!")) // conctenar o texto

